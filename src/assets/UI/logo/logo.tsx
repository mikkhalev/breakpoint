import React from 'react';
// @ts-ignore
import classes from '../css/logo.module.scss'
const Logo = () => {
    return (
        <div className={classes.logo}>
            the breakpoint.
        </div>
    );
};

export default Logo;